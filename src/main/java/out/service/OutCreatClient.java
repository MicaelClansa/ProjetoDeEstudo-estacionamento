package out.service;

import adapter.dto.ClientDTO;
import adapter.interfaces.ClientAdapter;

public class OutCreatClient implements ClientAdapter {

    @Override
    public void createClient(ClientDTO clientDTO) {
    }
}
