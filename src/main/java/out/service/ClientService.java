package out.service;

import adapter.dto.ClientDTO;
import adapter.interfaces.ClientAdapter;
import out.repository.ClientRepository;

public class ClientService implements ClientAdapter {

    @Override
    public void createClient(ClientDTO clientDTO) {

    }
}
