version: '3.1'

services:
  db:
    image: mysql:8.0
    volumes:
      - dbbase:/var/lib/mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: bancoTesteEstacionamento

volumes:
    dbbase: